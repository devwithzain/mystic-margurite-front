import { LoginForm } from "@/components";

export default function Login() {
	return (
		<div className="w-full h-screen flex items-center justify-center overflow-hidden absolute top-0 left-0 z-[2000] formBg">
			<LoginForm />
		</div>
	);
}
